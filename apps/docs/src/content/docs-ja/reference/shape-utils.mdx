---
title: "Shape Utils — @edv4h/usketch-shape-utils"
description: uSketch シェイププラグインを書くための共通ヘルパー。
order: 32
---

`@edv4h/usketch-shape-utils` は uSketch の shape プラグインを書くときに使う共通関数を提供する。React に依存しない純粋な関数群で、サードパーティが独自プラグイン（例: `@acme/usketch-plugin-shape-basic`）を作るときに、bounds 計算・ヒットテスト・リサイズ・テキスト編集・空き位置探索のような定型処理を再利用できる。

## Install

```bash
pnpm add @edv4h/usketch-shape-utils @edv4h/usketch-shared
```

## Bounds

```ts
function getBounds(data: ShapeData): BoundingBox;
```

shape の AABB を `{ x, y, width, height }` で返す。ほとんどの shape はこれをそのまま `ShapeDefinition.getBounds` に使える。

## Hit Test

```ts
function aabbHitTest(data: ShapeData, point: Point): boolean;
function ellipseHitTest(data: ShapeData, point: Point): boolean;
function lineHitTest(data: ShapeData, point: Point, tolerance?: number): boolean;
function pointInPolygon(point: Point, polygon: Point[]): boolean;
```

| 関数 | 用途 |
|------|------|
| `aabbHitTest` | 矩形 AABB |
| `ellipseHitTest` | 楕円 |
| `lineHitTest` | 線分（`tolerance` px 以内なら true、デフォルト 4） |
| `pointInPolygon` | 任意多角形（三角形・六角形・星形など） |

回転対応は `@edv4h/usketch-shared` の `withRotation(hitTest)` でラップする:

```tsx
import { withRotation } from "@edv4h/usketch-shared";
import { pointInPolygon } from "@edv4h/usketch-shape-utils";

const hitTest = withRotation((data, point) =>
  pointInPolygon(point, getHexagonPoints(data)),
);
```

## Resize

```ts
function createResize(minW: number, minH: number): (
  data: ShapeData,
  handle: ResizeHandle,
  delta: Point,
) => ShapeData;
```

8 方向（`se` / `nw` / `ne` / `sw` / `e` / `w` / `n` / `s`）のリサイズハンドルに対応した resize 関数を生成する。最小幅・最小高を指定。

```ts
import { createResize } from "@edv4h/usketch-shape-utils";

const resize = createResize(10, 10); // 最小 10x10 px
```

## Editable Text

`contentEditable` ベースのインプレース文字編集を shape プラグインに組み込むためのヘルパ。

```ts
function createEditableTextController(
  ctx: PluginContext,
  options: EditableTextOptions,
): EditableTextController;

function editableTextProps(id: string, text: string): { /* contentEditable div 用の props */ };

// controller が listen するカスタムイベント名
const TEXT_INPUT_EVENT: string;   // "usketch:text-input"
const TEXT_BLUR_EVENT: string;    // "usketch:text-blur"
const TEXT_ESCAPE_EVENT: string;  // "usketch:text-escape"
```

`createEditableTextController` は共有のテキスト編集ステートマシンと DOM/canvas リスナを配線し、`beginEdit` / `isEditing` / `teardown` を持つコントローラを返す。`editableTextProps` はエディタ用 `div`（HTML shape はプレーン div、SVG shape は `<foreignObject>` 内 div）へ spread する props を返し、IME 合成・Escape・blur・初回フォーカスを扱う。型 `EditableTextOptions` / `EditableTextController` も公開されている。

## Free Position

新しい shape を既存 shape と重ならない空き位置に配置するためのヘルパ。

```ts
function findFreePosition(opts: FindFreePositionOptions): BoundingBox;
function overlapsAny(box: BoundingBox, occupied: BoundingBox[]): boolean;

type FreePositionStrategy = "ring" | "push";
```

`findFreePosition` は希望位置（`desired`）と避ける矩形（`occupied`）から、`"ring"`（既定）または `"push"` 戦略で空き矩形を探索して返す。`overlapsAny` は矩形がいずれかの `occupied` と重なるかを判定する。型 `FindFreePositionOptions` / `FreePositionStrategy` も公開されている。

## GPU Primitive について

`rectGpuPrimitive` などの GPU primitive ヘルパはこのパッケージには**含まれていない**。これらは `@edv4h/usketch-plugin-shape-basic`（`plugins/usketch-plugin-shape-basic`）に属する。GPU 描画対応の詳細はそちらを参照。なお `ShapeDefinition.gpuPrimitive` フックそのものは各 shape 定義側で自由に実装できる。

## 関連

- [シェイププラグインを作る](../../guides/shape-plugin) — シェイププラグインの基本
- [サードパーティプラグインの作成](../../guides/third-party-plugin) — 外部パッケージとして shape-utils を使う完全ガイド
- [Shared Types](../shared) — `ShapeData` / `ResizeHandle` / `Point` 等の型定義
