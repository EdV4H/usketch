---
const features = [
	{
		title: "Plugin-first",
		jpTitle: "プラグイン・ファースト",
		desc: "Everything — shapes, tools, backgrounds, AI, presence — is a plugin with a typed SDK.",
		jpDesc: "シェイプ・ツール・背景・AI・プレゼンス、そのすべてが型付き SDK を備えたプラグイン。",
		accent: "violet",
	},
	{
		title: "Type-safe API",
		jpTitle: "型安全な API",
		desc: "Schemas with Zod, strict TS everywhere, no any. The same API used internally.",
		jpDesc: "スキーマは Zod、全面 strict TS、any 禁止。内部と同じ API を公開。",
		accent: "cyan",
	},
	{
		title: "Real-time collab",
		jpTitle: "リアルタイム協調",
		desc: "Yjs + Cloudflare Durable Objects. Ship multiplayer on day one.",
		jpDesc: "Yjs + Cloudflare Durable Objects。マルチプレイヤー対応を初日から。",
		accent: "pink",
	},
	{
		title: "AI-native",
		jpTitle: "AI ネイティブ",
		desc: "Copilot, Voice (NL2Canvas), Agent, Recognize — first-class canvas primitives for LLMs.",
		jpDesc: "Copilot / Voice (NL2Canvas) / Agent / Recognize — LLM 向けのキャンバスプリミティブ。",
		accent: "violet",
	},
];
---

<section class="feature-strip">
	<div class="feature-strip__inner">
		{features.map((f) => (
			<article class="feature" data-accent={f.accent}>
				<span class="feature__icon" aria-hidden="true"></span>
				<h3>
					<span data-lang="en">{f.title}</span>
					<span data-lang="jp">{f.jpTitle}</span>
				</h3>
				<p>
					<span data-lang="en">{f.desc}</span>
					<span data-lang="jp">{f.jpDesc}</span>
				</p>
			</article>
		))}
	</div>
</section>

<style>
	.feature-strip {
		padding: var(--space-12) 0;
	}
	.feature-strip__inner {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 var(--space-6);
		display: grid;
		grid-template-columns: repeat(4, minmax(0, 1fr));
		gap: var(--space-4);
	}
	.feature {
		padding: var(--space-5);
		background: var(--bg-surface);
		backdrop-filter: blur(20px) saturate(1.4);
		-webkit-backdrop-filter: blur(20px) saturate(1.4);
		border: 1px solid var(--border-subtle);
		border-radius: var(--r-lg);
	}
	.feature__icon {
		display: inline-flex;
		width: 28px;
		height: 28px;
		border-radius: 999px;
		margin-bottom: var(--space-3);
	}
	[data-accent="violet"] .feature__icon { background: linear-gradient(135deg, #8b5cf6, #6d28d9); }
	[data-accent="cyan"] .feature__icon { background: linear-gradient(135deg, #06b6d4, #0891b2); }
	[data-accent="pink"] .feature__icon { background: linear-gradient(135deg, #ec4899, #be185d); }
	.feature h3 {
		font-size: 15px;
		font-weight: 600;
		margin: 0 0 var(--space-2);
	}
	.feature p {
		font-size: 13px;
		line-height: 1.55;
		color: var(--fg-secondary);
		margin: 0;
	}
	@media (max-width: 880px) {
		.feature-strip__inner { grid-template-columns: repeat(2, 1fr); }
	}
	@media (max-width: 540px) {
		.feature-strip__inner { grid-template-columns: 1fr; }
	}
</style>
