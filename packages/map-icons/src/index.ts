// @edv4h/usketch-map-icons — the RPG map icon set as data, split out of the map
// plugin so hosts can enumerate / re-use the icons without pulling in the plugin.
export {
	ICON_CATEGORIES,
	ICONS,
	ICONS_BY_KEY,
	type IconCategory,
	type IconDef,
} from "./icons.js";
export type { SvgNode } from "./svg-node.js";
