/**
 * Default shape styles used across the application
 * These values should be consistent between tools and plugins
 */

export const DEFAULT_SHAPE_STYLES = {
	strokeColor: "#333333",
	fillColor: "#e0e0ff",
	strokeWidth: 2,
	opacity: 1,
} as const;

export const DEFAULT_FREEDRAW_STYLES = {
	strokeColor: "#333333",
	fillColor: "transparent",
	strokeWidth: 2,
	opacity: 1,
} as const;

export const DEFAULT_SHAPE_SIZE = {
	width: 100,
	height: 100,
} as const;
