---
"@edv4h/usketch-plugin-map": patch
---

拠点テリトリーの拡張が `emptyTerrain`（例: 海）のフォールバックを通ってしまい、未塗りの空マスまで「塗られている」扱いで連結が無限に広がる不具合を修正。拡張は**実際に塗られたマス**（tilemap.cells に存在するセル）のみを通すようにした。これによりビーコン設定時にテリトリーが表示されない/固まる問題を解消。`computeTerritory` の `empty` 引数を撤去。
